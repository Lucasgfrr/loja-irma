import 'package:flutter/material.dart';

class ConsumerProductPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Produtos Disponíveis'),
      ),
      body: ListView(
        children: [
          // Lista de produtos aqui
        ],
      ),
    );
  }
}
